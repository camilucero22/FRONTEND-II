# Live coding · Starter con Materialize CSS

El objetivo es construir una lista de intereses y trabajar arrays, índices, `length`, `for`, `for...of`, `forEach()`, funciones flecha, `push()`, `pop()` y DOM.

Materialize CSS se incorpora mediante CDN. Sus clases resuelven la presentación, pero toda la lógica de la aplicación se desarrolla con JavaScript.
